#Create the favorite_book() function, which takes title as its only parameter.
#The message that the function prints should read, "Alice in Wonderland is one of my favorite books."
#Include the book title as a parameter in the function call when you call it.

#Outlining the function
def favourite_book_name(title):
    print(f" {title} is one of my favorite books")

#Using the book title as a parameter when calling the function
favourite_book_name("Alice in Wonderland")